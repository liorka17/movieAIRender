<!DOCTYPE html>
<html lang="he">
<%- include('partials/head') %> <!-- חיבור ה-HEAD -->

<body>
    <%- include('partials/navbar') %> <!-- תפריט ניווט -->

    <main class="auth-container">
        <h2>📝 הרשמה</h2>

        <% if (typeof error !== 'undefined' && error) { %>
            <div class="error-message"><%= error %></div> <!-- הצגת הודעת שגיאה אם יש -->
        <% } %>

        <form action="/user/register" method="POST">
            <div class="input-group">
                <label for="username">👤 שם משתמש:</label>
                <input type="text" id="username" name="username" required>
            </div>
        
            <div class="input-group">
                <label for="email">📧 אימייל:</label>
                <input type="email" id="email" name="email" required>
            </div>
        
            <div class="input-group">
                <label for="password">🔒 סיסמה:</label>
                <input type="password" id="password" name="password" required>
            </div>
        
            <button type="submit" class="btn">✅ הירשם</button>
        </form>
        

        <p>כבר יש לך חשבון? <a href="/login">התחברות</a></p>
    </main>

    <%- include('partials/footer') %> <!-- פוטר -->
</body>
</html>
